"""Shared strategy state container (placeholder).

The state abstraction will bridge the simulator and adaptive policies once the
decision layer is restored.  Keeping the module allows incremental refactors
without breaking imports.
"""

